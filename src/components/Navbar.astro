---
/**
 * Navbar component
 *
 * Main navigation bar with logo and authentication button
 */

import { AuthButton } from "./AuthButton";
---

<nav class="bg-card/80 backdrop-blur-md border-b border-border/50 sticky top-0 z-50 shadow-soft">
  <div class="container mx-auto px-6 py-4 flex items-center justify-between">
    <!-- Logo -->
    <a href="/" class="font-display text-3xl font-bold text-primary hover:text-primary/80 transition-colors flex items-center gap-2">
      <span class="text-accent">🧘</span>
      YogaFit
    </a>

    <!-- Desktop Navigation -->
    <div class="hidden md:flex items-center gap-6">
      <AuthButton client:load />
    </div>

    <!-- Mobile Navigation -->
    <div class="md:hidden">
      <AuthButton client:load />
    </div>
  </div>
</nav>
