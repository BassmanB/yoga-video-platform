# 🔄 Authentication Flow Diagram

Wizualna reprezentacja przepływu autentykacji w aplikacji.

---

## 📊 Architecture Overview

```
┌─────────────────────────────────────────────────────────────┐
│                    YOGA VIDEO PLATFORM                       │
└─────────────────────────────────────────────────────────────┘

┌──────────────┐      ┌──────────────┐      ┌──────────────┐
│   Browser    │      │  Astro SSR   │      │  Supabase    │
│  (Client)    │◄────►│  (Server)    │◄────►│    Auth      │
└──────────────┘      └──────────────┘      └──────────────┘
      │                      │                      │
      │                      │                      │
   React              Middleware              Magic Link
 Components           Session Mgmt            Email Service
      │                      │                      │
 supabaseClient    createSupabaseServerClient   JWT Tokens
 (localStorage)         (cookies)              Session Mgmt
```

---

## 🔐 Magic Link Login Flow

```
┌─────────┐
│  USER   │
└────┬────┘
     │
     │ 1. Otwiera /auth/login
     ▼
┌─────────────────────────┐
│  login.astro (SERVER)   │
│                         │
│  if (Astro.locals.user) │──► Redirect → "/"
│    redirect("/")        │
│  else                   │
│    render LoginForm     │
└────────┬────────────────┘
         │
         │ 2. Render (client:load)
         ▼
┌─────────────────────────────────────────────┐
│  LoginForm.tsx (REACT)                      │
│                                             │
│  User wpisuje email: "user@example.com"     │
│  Klik "Wyślij link logowania"               │
│                                             │
│  const { error } = await                    │
│    supabaseClient.auth.signInWithOtp({      │
│      email: data.email                      │
│    })                                       │
└────────┬────────────────────────────────────┘
         │
         │ 3. HTTP POST
         ▼
┌─────────────────────────────────────────────┐
│  Supabase Auth API                          │
│                                             │
│  - Validate email                           │
│  - Generate magic link token                │
│  - Send email via SMTP                      │
│                                             │
│  Email Template:                            │
│  "Kliknij link aby się zalogować:          │
│   http://localhost:3000/auth/callback?     │
│   token_hash=ABC123&type=magiclink"        │
└────────┬────────────────────────────────────┘
         │
         │ 4. HTTP 200 OK
         ▼
┌─────────────────────────────────────────────┐
│  LoginForm.tsx (REACT)                      │
│                                             │
│  if (!error) {                              │
│    toast.success("Link wysłany!")           │
│    navigate("/auth/verify-email")           │
│  }                                          │
└────────┬────────────────────────────────────┘
         │
         │ 5. Redirect
         ▼
┌─────────────────────────────────────────────┐
│  verify-email.astro                         │
│                                             │
│  "Sprawdź swoją skrzynkę email"             │
│  "Wysłaliśmy link na: user@example.com"     │
│                                             │
│  [Wyślij ponownie] button                   │
└─────────────────────────────────────────────┘


         ⏱️  USER CZEKA NA EMAIL  ⏱️


┌─────────────────────────────────────────────┐
│  Email Inbox                                │
│                                             │
│  From: noreply@yourproject.supabase.co      │
│  Subject: Zaloguj się do YogaFit            │
│                                             │
│  Kliknij poniższy link:                     │
│  [Zaloguj się] ◄─── USER KLIKA              │
└────────┬────────────────────────────────────┘
         │
         │ 6. HTTP GET (magic link)
         ▼
┌─────────────────────────────────────────────┐
│  callback.astro (SERVER)                    │
│                                             │
│  const token_hash = Astro.url.searchParams  │
│    .get("token_hash")                       │
│                                             │
│  const { data, error } = await              │
│    supabase.auth.verifyOtp({                │
│      token_hash,                            │
│      type: "magiclink"                      │
│    })                                       │
│                                             │
│  if (!error) {                              │
│    // Session cookies set automatically     │
│    // by @supabase/ssr                      │
│    return Astro.redirect("/")               │
│  }                                          │
└────────┬────────────────────────────────────┘
         │
         │ 7. Set-Cookie headers
         │    sb-access-token=JWT...
         │    sb-refresh-token=...
         │
         │ 8. HTTP 302 Redirect → "/"
         ▼
┌─────────────────────────────────────────────┐
│  index.astro (SERVER)                       │
│                                             │
│  Middleware runs FIRST:                     │
│    - Reads cookies (sb-access-token)        │
│    - Calls supabase.auth.getUser()          │
│    - Sets Astro.locals.user = {...}         │
│    - Sets Astro.locals.userRole = "admin"   │
│                                             │
│  Page renders:                              │
│    - Navbar: shows user email + role        │
│    - VideoGrid: shows premium content       │
└─────────────────────────────────────────────┘

         ✅  USER ZALOGOWANY  ✅
```

---

## 🔄 Session Persistence Flow

```
┌────────────────────────────────────────────────┐
│  USER: Zamyka przeglądarkę, otwiera ponownie  │
└────────┬───────────────────────────────────────┘
         │
         │ 1. Otwiera stronę (np. /)
         ▼
┌─────────────────────────────────────────────────┐
│  Browser: Wysyła request z cookies              │
│                                                 │
│  GET / HTTP/1.1                                 │
│  Cookie: sb-access-token=JWT...;                │
│          sb-refresh-token=...                   │
└────────┬────────────────────────────────────────┘
         │
         │ 2. Request hits middleware
         ▼
┌─────────────────────────────────────────────────┐
│  Middleware (RUNS ON EVERY REQUEST)             │
│                                                 │
│  const supabase = createSupabaseServerClient({  │
│    headers: context.request.headers, // ← ma cookies │
│    cookies: context.cookies                     │
│  })                                             │
│                                                 │
│  const { data: { user } } = await               │
│    supabase.auth.getUser() // ← weryfikuje JWT  │
│                                                 │
│  context.locals.user = user                     │
│  context.locals.userRole = user.user_metadata.role │
└────────┬────────────────────────────────────────┘
         │
         │ 3. Page renders z user context
         ▼
┌─────────────────────────────────────────────────┐
│  Page Component                                 │
│                                                 │
│  const user = Astro.locals.user // NOT NULL ✅  │
│  const role = Astro.locals.userRole // "admin"  │
│                                                 │
│  Navbar: shows user info                        │
└─────────────────────────────────────────────────┘

         ✅  SESSION PERSISTS  ✅

┌────────────────────────────────────────────────┐
│  Token Expiry Handling                         │
│                                                │
│  Access Token: Valid 1 hour                    │
│  Refresh Token: Valid 30 days                  │
│                                                │
│  When access token expires:                    │
│    1. Supabase SDK detects expired token       │
│    2. Automatically calls refresh endpoint     │
│    3. Gets new access token (using refresh)    │
│    4. Updates cookies                          │
│    5. User STAYS LOGGED IN ✅                  │
│                                                │
│  When refresh token expires (after 30 days):   │
│    1. User must login again                    │
└────────────────────────────────────────────────┘
```

---

## 🚪 Logout Flow

```
┌─────────┐
│  USER   │ Klika avatar → "Wyloguj się"
└────┬────┘
     │
     │ 1. onClick handler
     ▼
┌─────────────────────────────────────────────┐
│  AuthButton.tsx (REACT)                     │
│                                             │
│  const { signOut } = useAuth()              │
│                                             │
│  const handleSignOut = async () => {        │
│    await signOut()                          │
│  }                                          │
└────────┬────────────────────────────────────┘
         │
         │ 2. Calls useAuth hook
         ▼
┌─────────────────────────────────────────────┐
│  useAuth.ts (REACT)                         │
│                                             │
│  const signOut = async () => {              │
│    const { error } = await                  │
│      supabase.auth.signOut()                │
│  }                                          │
└────────┬────────────────────────────────────┘
         │
         │ 3. HTTP POST to Supabase
         ▼
┌─────────────────────────────────────────────┐
│  Supabase Auth API                          │
│                                             │
│  - Invalidate session                       │
│  - Blacklist refresh token                  │
│  - Return success                           │
└────────┬────────────────────────────────────┘
         │
         │ 4. HTTP 200 OK
         ▼
┌─────────────────────────────────────────────┐
│  Browser: Deletes cookies automatically     │
│                                             │
│  Delete: sb-access-token                    │
│  Delete: sb-refresh-token                   │
└────────┬────────────────────────────────────┘
         │
         │ 5. Auth state change event
         ▼
┌─────────────────────────────────────────────┐
│  useAuth.ts (REACT)                         │
│                                             │
│  supabase.auth.onAuthStateChange(           │
│    (_event, session) => {                   │
│      setUser(session?.user ?? null)         │
│    }                                        │
│  )                                          │
│                                             │
│  → setUser(null)                            │
└────────┬────────────────────────────────────┘
         │
         │ 6. Re-render
         ▼
┌─────────────────────────────────────────────┐
│  AuthButton.tsx (REACT)                     │
│                                             │
│  user === null                              │
│                                             │
│  Shows: "Zaloguj się" button                │
└─────────────────────────────────────────────┘

         ✅  USER WYLOGOWANY  ✅
```

---

## 🛡️ Protected Route Flow

```
┌─────────┐
│  USER   │ Free user próbuje obejrzeć premium video
└────┬────┘
     │
     │ 1. Klika premium video card
     ▼
┌─────────────────────────────────────────────┐
│  VideoCard.tsx                              │
│                                             │
│  <a href={`/video/${video.id}`}>            │
│    {video.is_premium && <Badge>Premium</Badge>} │
│  </a>                                       │
└────────┬────────────────────────────────────┘
         │
         │ 2. Navigate to /video/premium-123
         ▼
┌─────────────────────────────────────────────┐
│  Middleware (AUTO RUNS)                     │
│                                             │
│  const user = await supabase.auth.getUser() │
│  context.locals.user = user                 │
│  context.locals.userRole = "free"           │
│                                             │
│  return next() // ← Doesn't block!          │
└────────┬────────────────────────────────────┘
         │
         │ 3. Route handler
         ▼
┌─────────────────────────────────────────────┐
│  /video/[id].astro (SERVER)                 │
│                                             │
│  const { id } = Astro.params                │
│  const user = Astro.locals.user             │
│  const userRole = Astro.locals.userRole     │
│                                             │
│  // Fetch video                             │
│  const video = await fetchVideo(id)         │
│                                             │
│  // Check access                            │
│  if (video.is_premium && !user) {           │
│    return Astro.redirect(                   │
│      `/auth/login?redirect=/video/${id}`    │
│    )                                        │
│  }                                          │
│                                             │
│  // Pass userRole to component              │
│  <VideoPlayerContainer                      │
│    video={video}                            │
│    userRole={userRole}                      │
│  />                                         │
└────────┬────────────────────────────────────┘
         │
         │ 4. React component decides
         ▼
┌─────────────────────────────────────────────┐
│  VideoPlayerContainer.tsx                   │
│                                             │
│  if (video.is_premium &&                    │
│      userRole !== "premium" &&              │
│      userRole !== "admin") {                │
│                                             │
│    return <PremiumGate />                   │
│  }                                          │
│                                             │
│  return <VideoPlayer video={video} />       │
└─────────────────────────────────────────────┘

         🔒  ACCESS DENIED - PREMIUM GATE  🔒
```

---

## 📦 Component Communication

```
┌────────────────────────────────────────────────────────┐
│                    REQUEST LIFECYCLE                    │
└────────────────────────────────────────────────────────┘

Browser Request
    │
    ▼
┌─────────────────────────────────────────┐
│  Middleware (FIRST, ALWAYS RUNS)        │
│                                         │
│  Input:  Headers + Cookies              │
│  Action: Create Supabase server client  │
│          Get user session               │
│  Output: Astro.locals.user              │
│          Astro.locals.userRole          │
│          Astro.locals.supabase          │
└────┬────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│  Astro Page/API Route (SERVER)          │
│                                         │
│  Has access to:                         │
│    - Astro.locals.user                  │
│    - Astro.locals.userRole              │
│    - Astro.locals.supabase              │
│                                         │
│  Can:                                   │
│    - Check auth (if user logged in)     │
│    - Redirect to login if needed        │
│    - Fetch data with RLS                │
│    - Pass props to React components     │
└────┬────────────────────────────────────┘
     │
     ▼
┌─────────────────────────────────────────┐
│  React Component (BROWSER)              │
│                                         │
│  Receives props from Astro:             │
│    - userRole="free"                    │
│    - video={...}                        │
│                                         │
│  Has access to:                         │
│    - supabaseClient (from import)       │
│    - useAuth() hook                     │
│                                         │
│  Can:                                   │
│    - Call auth methods (signOut, etc.)  │
│    - Listen to auth state changes       │
│    - Show/hide UI based on auth         │
└─────────────────────────────────────────┘
```

---

## 🔧 Data Flow: Server vs Client

```
┌──────────────────────────────────────────────────────────┐
│                      SERVER-SIDE                         │
│                   (Astro SSR - Node)                     │
└──────────────────────────────────────────────────────────┘

createSupabaseServerClient()
          │
          ├─► Reads: Headers (Cookie: sb-access-token=...)
          │
          ├─► Session Storage: COOKIES
          │   ├─ getAll() → parse Cookie header
          │   └─ setAll() → context.cookies.set()
          │
          ├─► Used in:
          │   ├─ Middleware
          │   ├─ Astro pages (*.astro)
          │   └─ API routes (/api/*)
          │
          └─► Security:
              ├─ httpOnly: true (XSS safe)
              ├─ secure: true (HTTPS only)
              └─ sameSite: "lax" (CSRF safe)


┌──────────────────────────────────────────────────────────┐
│                     CLIENT-SIDE                          │
│                  (React - Browser)                       │
└──────────────────────────────────────────────────────────┘

supabaseClient (createBrowserClient)
          │
          ├─► Session Storage: localStorage
          │   ├─ Auto-sync with cookies
          │   └─ Fallback for client-side ops
          │
          ├─► Used in:
          │   ├─ React components (*.tsx)
          │   ├─ useAuth() hook
          │   └─ LoginForm, AuthButton, etc.
          │
          └─► Features:
              ├─ Auto-refresh session
              ├─ Auth state listeners
              └─ signOut(), signInWithOtp()
```

---

## 🎯 Security Layers

```
┌─────────────────────────────────────────────────────┐
│  Layer 1: NETWORK (HTTPS)                           │
│                                                     │
│  ✅ Secure cookies only over HTTPS in production    │
│  ✅ Man-in-the-middle prevention                    │
└─────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│  Layer 2: COOKIES (httpOnly, sameSite)              │
│                                                     │
│  ✅ httpOnly: true → JavaScript can't access        │
│  ✅ sameSite: "lax" → CSRF protection               │
│  ✅ 30-day expiry → Auto-cleanup                    │
└─────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│  Layer 3: SERVER-SIDE VALIDATION (Middleware)       │
│                                                     │
│  ✅ Every request → supabase.auth.getUser()         │
│  ✅ JWT validation (Supabase verifies)              │
│  ✅ No client-side bypass possible                  │
└─────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│  Layer 4: ROUTE-LEVEL GUARDS (Astro pages)          │
│                                                     │
│  ✅ if (!user) → redirect to login                  │
│  ✅ if (userRole !== "premium") → show gate         │
│  ✅ Server-side decision → can't be faked           │
└─────────────────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────┐
│  Layer 5: DATABASE (RLS Policies)                   │
│                                                     │
│  ✅ Even if attacker bypasses frontend...           │
│  ✅ Database enforces access rules                  │
│  ✅ auth.jwt() -> 'user_metadata' ->> 'role'        │
└─────────────────────────────────────────────────────┘
```

---

## 📈 Performance Characteristics

```
┌──────────────────────────────────────────────────┐
│  Operation              │  Time   │  Caching    │
├──────────────────────────────────────────────────┤
│  Middleware (session)   │  ~20ms  │  Per request│
│  Login (magic link)     │  ~500ms │  N/A        │
│  Email delivery         │  ~3s    │  N/A        │
│  Session refresh        │  ~100ms │  Auto       │
│  Logout                 │  ~200ms │  N/A        │
└──────────────────────────────────────────────────┘

Bottlenecks:
  1. Email delivery (Supabase SMTP) - 3-5s
     → Solution: Custom SMTP for production

  2. Middleware on every request - ~20ms
     → Acceptable overhead, necessary for security

  3. Token refresh - ~100ms
     → Handled automatically by SDK
```

---

## 🔄 State Synchronization

```
┌────────────────────────────────────────────────┐
│  SERVER STATE (Source of Truth)                │
│                                                │
│  Astro.locals.user = {...}                     │
│  Astro.locals.userRole = "premium"             │
│                                                │
│  Updated: Every request (middleware)           │
│  Storage: Request context (ephemeral)          │
└────────┬───────────────────────────────────────┘
         │
         │ Props passing
         ▼
┌────────────────────────────────────────────────┐
│  CLIENT STATE (Synchronized)                   │
│                                                │
│  useAuth() → user = {...}                      │
│  useAuth() → role = "premium"                  │
│                                                │
│  Updated: Auth state change events             │
│  Storage: React state + localStorage           │
└────────────────────────────────────────────────┘

Sync mechanism:
  1. Server renders with Astro.locals.user
  2. Props passed to React: <Component user={user} />
  3. React hydrates with server data
  4. useAuth() syncs with Supabase client
  5. onAuthStateChange() keeps React updated
```

---

## 🎨 Visual Summary

```
┌─────────────────────────────────────────────────────────┐
│                 AUTHENTICATION ARCHITECTURE              │
└─────────────────────────────────────────────────────────┘

                    ┌──────────────┐
                    │   Supabase   │
                    │     Auth     │
                    └──────┬───────┘
                           │
            ┌──────────────┼──────────────┐
            │              │              │
      Magic Link     JWT Tokens     Email Service
            │              │              │
            ▼              ▼              ▼
    ┌──────────────────────────────────────────┐
    │         Astro Server (Node/SSR)          │
    │                                          │
    │  ┌────────────────────────────────────┐  │
    │  │  Middleware (Session Provider)     │  │
    │  │                                    │  │
    │  │  - createSupabaseServerClient()    │  │
    │  │  - getUser() from cookies          │  │
    │  │  - Set Astro.locals.user           │  │
    │  │  - Set Astro.locals.userRole       │  │
    │  └────────────────────────────────────┘  │
    │                    │                     │
    │         ┌──────────┼──────────┐          │
    │         ▼          ▼          ▼          │
    │   /auth/login  /video/[id]  /api/*      │
    │         │          │          │          │
    │    (renders)  (checks auth) (protected) │
    └─────────┼──────────┼──────────┼──────────┘
              │          │          │
              ▼          ▼          ▼
    ┌──────────────────────────────────────────┐
    │         Browser (React Islands)          │
    │                                          │
    │  ┌────────────────────────────────────┐  │
    │  │  LoginForm.tsx                     │  │
    │  │  - signInWithOtp()                 │  │
    │  │  - Toast notifications             │  │
    │  │  - Email enumeration prevention    │  │
    │  └────────────────────────────────────┘  │
    │                                          │
    │  ┌────────────────────────────────────┐  │
    │  │  useAuth() Hook                    │  │
    │  │  - supabaseClient (browser)        │  │
    │  │  - Auth state listener             │  │
    │  │  - signOut()                       │  │
    │  └────────────────────────────────────┘  │
    │                                          │
    │  ┌────────────────────────────────────┐  │
    │  │  AuthButton.tsx                    │  │
    │  │  - Shows user email + role         │  │
    │  │  - Logout button                   │  │
    │  └────────────────────────────────────┘  │
    └──────────────────────────────────────────┘
```

---

## 📚 Key Takeaways

1. **Two Clients:**
   - Server: `createSupabaseServerClient()` (cookies)
   - Browser: `supabaseClient` (localStorage)

2. **Middleware:**
   - Runs on EVERY request
   - Provides session to all routes
   - Does NOT block requests (session provider)

3. **Security:**
   - httpOnly cookies (XSS protection)
   - Server-side validation (can't bypass)
   - Email enumeration prevention (always sukces)

4. **Session:**
   - Persists 30 days (refresh token)
   - Auto-refresh before expiry
   - Synchronized server ↔ client

5. **Flow:**
   - Magic link → Email → Callback → Session → Logged in
   - Session persists → Browser restart → Still logged in
   - Logout → Cookies deleted → Guest state

---

**End of Diagram** 🎯
